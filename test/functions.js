export function createCountString(cityCounter, styleCounter, activityCounter) {
    return `You have changed the city ${cityCounter} times, the style ${styleCounter} times, and the activity ${activityCounter} times. And your slogan is cool.`;
}
